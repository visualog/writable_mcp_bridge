# Xbridge QA Report - 2026-05-29 17:59 KST

## Result

**Release gate: PASS**

Buddy-style UX/work-console expectations, Figma official-guide contracts, live bridge transport, live Figma read/write paths, token export, and regression tests were verified against the local Xbridge server and the active Figma file.

## Environment

- Workspace: `/Users/im_018/Documents/GitHub/Project/figma_skills/xbridge`
- Server: `writable-mcp-bridge` `0.5.65`
- Figma file: `Agent_skill_test`
- Active plugin id: `page:33276:16484`
- Active page: `Page 55` (`33276:16484`)
- QA frame created in Figma: `Xbridge QA PASS - 20260529-UX-FUNC` (`33416:98`)

## Findings Fixed During QA

- Full token export with aliases/resolved values previously timed out with default chunk size.
- Fix applied:
  - Default `EXPORT_DESIGN_TOKENS_CHUNK_LIMIT` lowered from `100` to `20`.
  - `/api/export-design-tokens` now accepts `chunkLimit` as an alias for `limit`.
- Evidence after fix:
  - Full token export: 7 collections, 548 variables, 890ms, no warnings.

## Gate Evidence

### Official Figma Guide Contracts

- `documentAccess: "dynamic-page"` present.
- `networkAccess.allowedDomains` stays `["none"]`.
- localhost/ws bridge access remains in `devAllowedDomains`.
- Plugin main thread avoids browser networking/timer APIs.
- Dynamic-page read paths and async variable/style APIs are covered by contract tests.

Command:

```bash
node --test tests/token-export-contract.test.js tests/version-consistency.test.js
```

Result: 18 pass, 0 fail before the token-export fix; 17 pass, 0 fail for token contract after the fix.

### Live Bridge / Transport

Commands:

```bash
node scripts/agent-preflight.mjs
node scripts/validate-streaming-first.mjs
node scripts/validate-streaming-first-soak.mjs --profile=quick
```

Results:

- Agent preflight: pass.
- Streaming validator: pass.
- Quick soak: 2/2 pass.
- Final `/health`: `transportHealth.grade=healthy`, `commandReadiness.status=ready`, `writeReadiness.status=ready`.
- Final queue: `pendingTotal=0`, `pendingResultsTotal=0`, `undeliveredTotal=0`.

### Live Figma Read/Write

Read checks:

- `/api/pages?pluginId=page:33276:16484` returned current page `Page 55`.
- `/api/get-selection` returned selected node `33392:3972000` / `npay_asset_08 2`.
- `/api/get-node-details` confirmed the QA frame and children.

Write checks:

- Created QA root frame `33416:98`.
- Created 6 editable child nodes: text, rectangle button background, toggle track/knob.
- Bulk-updated 3 text nodes.
- Built a nested generated mobile screen under the QA frame with editable FRAME/TEXT/RECTANGLE structure.
- Renamed the QA frame to `Xbridge QA PASS - 20260529-UX-FUNC`.

### Image Analysis / No-Build Behavior

Request:

```text
선택한 이미지를 분석하고 품질 진단만 해줘
```

Result:

- `ok=true`
- `intentKind=inspect_selection`
- `userIntentKind=image_analysis_only`
- `internalIntentKind=inspect_selection`
- `imageAnalysis` returned structured summary, canvas spec, layout map, role map, text style map, semantic quality.
- No Figma build result was created for analysis-only mode.

### Token Export

Initial failing evidence:

- Full export with default chunk size returned partial artifact:
  - 7 collections
  - 0 variables
  - warning: `Timed out waiting for plugin response: export_design_tokens_chunk`

After fix:

```bash
curl -s -X POST http://127.0.0.1:3846/api/export-design-tokens \
  -H 'Content-Type: application/json' \
  -d '{"pluginId":"page:33276:16484","includeAliases":true,"includeResolvedValues":true,"includeStyles":true}'
```

Result:

- Artifact: `/private/tmp/xbridge-token-exports/Agent_skill_test-2026-05-29T08-56-57-401Z.json`
- Collections: 7
- Variables: 548
- Elapsed: 890ms
- Warnings: none

### Regression Tests

Commands:

```bash
node --check src/server.js
node --check src/ai-designer-intents.js
node --check src/codex-cli-runner.js
node --check scripts/validate-streaming-first.mjs
node --test tests/token-export-contract.test.js tests/ai-designer-intents.test.js tests/ui-designer-contract.test.js tests/codex-cli-runner.test.js
node --test tests/ai-designer-chat-api.integration.test.js
npm test
```

Results:

- Static checks: pass.
- Targeted token/intent/UI/Codex runner tests: 100 pass, 0 fail.
- AI designer chat integration: 18 pass, 12 skipped, 0 fail.
- `npm test`: 529 tests, 517 pass, 12 skipped, 0 fail.

## UX/UI Checklist

- Work-console progress contracts are covered by `tests/ui-designer-contract.test.js`.
- Image generation progress ends at `생성 가능 여부 확인`, not misleading `화면 레이어 생성`.
- Understructured image failures include cause and retry guidance.
- Analysis-only requests do not create Figma layers.
- Result and failure copy remains user-actionable rather than transport-only.

## Remaining Notes

- The QA run created a persistent evidence frame in `Page 55`: `Xbridge QA PASS - 20260529-UX-FUNC`.
- The first create-node attempt without explicit `parentId` failed after validator sessions because the default parent resolved to a validation session label. Live write tests used explicit `parentId`, and the final active session recovered to a single live Figma session. This is worth monitoring if future QA tools leave synthetic sessions active.
- `ELLIPSE` is not a supported `create_node` type in the current contract; toggle knob QA used a rounded `RECTANGLE` instead.
